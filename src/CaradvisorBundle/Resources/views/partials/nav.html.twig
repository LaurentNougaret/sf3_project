{% block body %}
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
        <div class="navbar-header page-scroll">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand page-scroll" href="{{ path('home') }}"><p><span class="simplelogo1">Car</span><span class="simplelogo2">Advisor</span></p></a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if is_granted('ROLE_PRO') %}
                    <li>
                        <a>Bonjour {{ app.user.userName }}</a>
                    </li>
                    <li>
                        <a href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li>
                        <a href="{{ path('user') }}">Mon Compte</a>
                    </li>
                    <li>
                        <a href="{{ path('logout') }}">Deconnexion</a>
                    </li>
                {% elseif is_granted('ROLE_PART') %}
                    <li>
                        <a>Bonjour {{ app.user.userName }}</a>
                    </li>
                    <li>
                        <a href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li>
                        <a class="page-scroll" href="#review">Déposez un avis</a>
                    </li>
                    <li>
                        <a href="{{ path('user')}}">Mon Compte</a>
                    </li>
                    <li>
                        <a href="{{ path('logout') }}">Deconnexion</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li>
                        <a class="page-scroll" href="#review">Déposez un avis</a>
                    </li>
                    <li>
                        <a href="{{ path('user_signup') }}">Inscription</a>
                    </li>
                    <li>
                        <a href="{{ path('login') }}">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
    {% include ('@Caradvisor/partials/pro_signin.html.twig')%}
    {% include ('@Caradvisor/partials/user_signin.html.twig')%}
{% endblock %}
